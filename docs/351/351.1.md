# 351.1 – Virtualization Concepts and Theory

> **Weight:** 6
>
> **Estimated Study Time:** 90–120 minutes
>
> **Personal Priority:** ★★★★★
>
> **Lab Required:** Yes (Discovery Lab)

---

# Official Objective

## Objective Summary

This objective introduces the fundamental concepts of virtualization and the terminology required to understand modern virtualization platforms.

It explains the different virtualization architectures, the relationship between the major software components and the basic concepts used throughout the remaining objectives.

> **Note**
>
> This section intentionally summarizes the official LPIC-3 305 objective.
> It is intended as an introduction and does not replace the official wording.

---

# Learning Outcomes

After completing this chapter you can:

- explain why virtualization exists
- distinguish Type 1 and Type 2 hypervisors
- distinguish Hypervisor, Virtual Machine Monitor (VMM) and Management Platform
- explain the roles of KVM, QEMU, libvirt and Proxmox VE
- explain Full Virtualization and Paravirtualization
- explain Hardware-assisted Virtualization
- understand the terminology used throughout the remaining objectives

---

# Why Virtualization?

Modern servers are rarely dedicated to a single operating system.

Virtualization allows multiple isolated operating systems to share the same physical hardware while remaining independent from each other.

Typical benefits include:

- better hardware utilization
- workload consolidation
- isolation
- snapshots
- live migration
- easier backup and disaster recovery
- testing environments

> **Exam Tip**
>
> LPIC rarely asks *why* virtualization exists.
> However, understanding the motivation makes many later concepts much easier to understand.

---

# What is a Virtual Machine?

A **Virtual Machine (VM)** is a software-defined computer that behaves like a physical computer.

From the perspective of the operating system running inside the VM (the **guest operating system**), the virtual machine appears to provide its own hardware, including:

- one or more virtual CPUs (vCPUs)
- virtual memory (RAM)
- virtual storage devices
- virtual network interfaces
- virtual firmware (BIOS or UEFI)

Although these resources appear to belong exclusively to the virtual machine, they are provided by the virtualization software and ultimately backed by the physical hardware of the host system.

This allows multiple isolated operating systems to run simultaneously on a single physical computer.

---

## Host and Guest

Virtualization introduces two important terms:

- **Host**: The physical system that provides CPU, memory, storage and other hardware resources.
- **Guest**: The operating system running inside a virtual machine.

One host system can run multiple guest operating systems at the same time.

---

## Virtual Hardware

A guest operating system interacts with virtual hardware in the same way it would interact with physical hardware.

It interacts with devices that are presented by the virtualization platform, such as:

- virtual CPU
- virtual RAM
- virtual disk
- virtual network adapter

The virtualization layer translates these requests to the underlying physical hardware.

---

> **Exam Tip**
>
> A virtual machine is **not the same as an emulator**.
>
> Modern virtualization usually executes guest CPU instructions directly on the physical processor using hardware-assisted virtualization, while only selected hardware devices may be emulated when required.

---

> **Common Mistake**
>
> A virtual machine is **not** simply a collection of files.
>
> The virtual disk (`.qcow2`, `.vmdk`, etc.) is only one part of a VM. CPU state, memory allocation, virtual devices and configuration are equally important parts of the virtual machine.

---

## Review

After reading this section you should be able to answer the following questions:

- [ ] What is a virtual machine?
- [ ] What is the difference between a host and a guest?
- [ ] Which hardware components are typically virtualized?
- [ ] Why can multiple guest operating systems run on the same physical host?
- [ ] What is the difference between a physical machine and a virtual machine?

---

# Linux Virtualization Stack

One of the most common misconceptions is assuming that KVM, QEMU, libvirt and Proxmox perform the same task.

Each component has a distinct responsibility. Together these components form the Linux virtualization stack.

| Component  | Layer               | Purpose                                       |
|------------|---------------------|-----------------------------------------------|
| KVM        | Kernel              | Hardware-assisted virtualization              |
| QEMU       | Userspace           | Virtual machine process and device emulation  |
| libvirt    | Management          | Virtualization API and daemon                 |
| Proxmox VE | Management Platform | Management of virtual machines and containers |

> **Common Mistake**
>
> Do not confuse KVM, QEMU, libvirt and Proxmox.
> They are complementary components, not competing products.

---

# Architecture

```text
                     Proxmox VE
                           │
              +------------+------------+
              │                         │
           Web GUI                  qm CLI
              │                         │
              +----------- API ---------+
                          │
                    QEMU process
                          │
                      /dev/kvm
                          │
                    Linux Kernel
                          │
               Intel VT-x / AMD-V
                          │
                      CPU Hardware
```

Understanding this architecture is one of the most important learning goals of the LPIC-3 virtualization objectives.

---

# Hypervisor Types

A **hypervisor** is the software layer responsible for creating, running and managing virtual machines.

It provides guest operating systems with access to virtual hardware while controlling how physical hardware resources are shared between multiple virtual machines.

Without a hypervisor, multiple operating systems cannot run simultaneously in isolated virtual machines on the same physical host.

---

## Hypervisor vs. Virtual Machine Monitor (VMM)

The terms **Hypervisor** and **Virtual Machine Monitor (VMM)** are often used interchangeably.

Historically, a **Virtual Machine Monitor (VMM)** referred to the software component responsible for monitoring and managing virtual machines.

Today, the term **hypervisor** is more common and usually describes the same concept.

For the LPIC-3 exam, both terms can generally be treated as equivalent unless a question explicitly distinguishes between them.

---

## Type 1 Hypervisors

A **Type 1 Hypervisor**, also called a **bare-metal hypervisor**, runs directly on the physical hardware.

There is no general-purpose host operating system between the hardware and the hypervisor.

Examples include:

- VMware ESXi
- Xen
- Microsoft Hyper-V
- Citrix Hypervisor

Type 1 hypervisors are commonly used in data centers because they provide high performance and efficient resource management.

---

## Type 2 Hypervisors

A **Type 2 Hypervisor** runs as an application on top of a host operating system.

The host operating system is responsible for managing the physical hardware and device drivers.

Examples include:

- Oracle VirtualBox
- VMware Workstation
- VMware Fusion
- Parallels Desktop

Type 2 hypervisors are commonly used for development, testing and desktop virtualization.

---

## Comparison

| Feature | Type 1 | Type 2 |
|---------|--------|--------|
| Runs on | Physical hardware | Host operating system |
| Typical use | Servers, data centers | Desktop systems |
| Performance | Higher | Slightly lower |
| Examples | ESXi, Xen | VirtualBox, VMware Workstation |

---

> **Exam Tip**
>
> The difference between Type 1 and Type 2 hypervisors is determined by **where the hypervisor runs**, not by which virtualization technology it uses.

---

> **Common Mistake**
>
> Do not confuse a **hypervisor** with a **management platform**.
>
> Products such as **Proxmox VE** or **VMware vCenter** provide management capabilities but are not themselves the hypervisor.

---

## Review

After reading this section you should be able to answer the following questions:

- [ ] What is a hypervisor?
- [ ] What is the difference between a hypervisor and a VMM?
- [ ] What distinguishes a Type 1 from a Type 2 hypervisor?
- [ ] Which products are examples of Type 1 and Type 2 hypervisors?
- [ ] Why is Proxmox VE not itself a hypervisor?

---

# Components

## KVM

**KVM** stands for **Kernel-based Virtual Machine**.

KVM is the virtualization facility built into the Linux kernel. It enables Linux to use the hardware-assisted virtualization features provided by modern processors.

Unlike complete virtualization products, KVM focuses on CPU virtualization and works together with QEMU to provide a complete virtual machine.

---

### Kernel Modules

KVM is implemented as Linux kernel modules.

The most common modules are:

- `kvm`
- `kvm_intel`
- `kvm_amd`

The CPU-specific module depends on the processor vendor:

- Intel processors use `kvm_intel`
- AMD processors use `kvm_amd`

---

### Hardware-assisted Virtualization

KVM relies on CPU virtualization extensions.

The relevant technologies are:

- **Intel VT-x**
- **AMD-V**

These processor features allow guest operating systems to execute CPU instructions efficiently on the physical processor.

> **Common Mistake**
>
> Do not confuse **Intel VT-x** with **Intel VT-d**.
>
> - **VT-x** provides CPU virtualization.
> - **VT-d** provides I/O virtualization (PCI passthrough via the IOMMU).

---

### `/dev/kvm`

KVM exposes its functionality through the device file:

`/dev/kvm`

Applications running in userspace, such as QEMU, use this interface to communicate with the KVM subsystem inside the Linux kernel.

---

### Relationship to QEMU

KVM and QEMU have different responsibilities.

| Component | Responsibility |
|-----------|----------------|
| KVM | Hardware-assisted CPU virtualization |
| QEMU | Virtual machine process and virtual hardware |

Together they form the standard Linux virtualization solution.

```text
QEMU
  │
  ▼
/dev/kvm
  │
  ▼
Linux Kernel + KVM
  │
  ▼
Intel VT-x / AMD-V
```

Without KVM, QEMU can still emulate an entire computer, but performance is significantly lower because CPU instructions must be emulated instead of executed directly by the processor.

---

### Is KVM a Hypervisor?

KVM is commonly regarded as the **hypervisor component** of the Linux virtualization stack.

At first glance, Linux virtualization looks different from a traditional Type 1 hypervisor because the Linux kernel is visible in the architecture.

However, the Linux kernel together with KVM forms the hypervisor layer.

A typical Linux virtualization environment combines:

- **Linux Kernel** as the operating system foundation
- **KVM** for hardware-assisted CPU virtualization
- **QEMU** for the virtual machine process and virtual hardware

The following diagrams illustrate the difference.

```text
Classic Type 1 Hypervisor

+----------------------+
| Guest Operating Sys. |
+----------------------+
|      Hypervisor      |
+----------------------+
|      Hardware        |
+----------------------+
```

```text
Linux + KVM

+----------------------+
| Guest Operating Sys. |
+----------------------+
|        QEMU          |
+----------------------+
| Linux Kernel + KVM   |
+----------------------+
|      Hardware        |
+----------------------+
```

Although the Linux architecture looks different, the Linux kernel together with KVM provides the hypervisor functionality.

QEMU runs in userspace and provides the virtual machine process, virtual hardware and device emulation.

> **Exam Tip**
>
> For the LPIC-3 exam, KVM is considered a **Type 1 hypervisor** because virtualization is performed by the Linux kernel directly on the hardware using Intel VT-x or AMD-V.

---

### Verification Commands

Show loaded KVM modules:

```bash
lsmod | grep kvm
```

Check whether the KVM device exists:

```bash
ls -l /dev/kvm
```

Check whether the processor supports hardware virtualization:

```bash
grep -E 'vmx|svm' /proc/cpuinfo
```

Meaning:

- `vmx` = Intel VT-x
- `svm` = AMD-V

---

> **Exam Tip**
>
> If an exam question asks which Linux kernel component provides hardware-assisted virtualization, the correct answer is **KVM**.
>
> If it asks which component creates and runs the virtual machine process, the answer is **QEMU**.

---

### Review

After reading this section you should be able to answer the following questions:

- [ ] What does KVM stand for?
- [ ] Which Linux kernel modules belong to KVM?
- [ ] Which CPU technologies are required for KVM?
- [ ] What is the purpose of `/dev/kvm`?
- [ ] Why are KVM and QEMU commonly used together?
- [ ] Why is KVM considered the hypervisor component of Linux?

---

## QEMU

**QEMU** stands for **Quick Emulator**.

QEMU is a userspace application that creates and runs virtual machines.

It provides the complete virtual machine process, including virtual hardware such as CPUs, memory, storage controllers, network adapters, firmware and other devices.

When hardware-assisted virtualization is available, QEMU delegates CPU virtualization to KVM while continuing to manage the virtual machine itself.

---

### Responsibilities

QEMU is responsible for creating and managing the virtual machine.

Typical responsibilities include:

- creating the virtual machine process
- allocating virtual memory
- presenting virtual CPUs
- emulating virtual hardware devices
- providing virtual firmware (BIOS or UEFI)
- attaching virtual disks
- attaching virtual network interfaces
- handling virtual input and output devices

Unlike KVM, QEMU provides the complete virtual hardware environment required by a guest operating system.

---

### Relationship to KVM

QEMU and KVM work together.

Each component has a different responsibility.

| Component | Responsibility |
|-----------|----------------|
| KVM | Hardware-assisted CPU virtualization |
| QEMU | Virtual machine process and virtual hardware |

```text
                Guest Operating System
                         │
                 Virtual Hardware
                         │
                      QEMU
                         │
                     /dev/kvm
                         │
                 Linux Kernel + KVM
                         │
                     CPU Hardware
```

When KVM is available, guest CPU instructions are executed directly by the physical processor.

Without KVM, QEMU performs complete software emulation of the processor, which is considerably slower.

---

### Emulation vs. Virtualization

One of QEMU's unique strengths is that it can operate in two different modes.

#### QEMU with KVM

```text
QEMU + KVM
```

- hardware-assisted virtualization
- near-native CPU performance
- commonly used in production environments

#### QEMU without KVM

```text
QEMU only
```

- complete CPU emulation
- significantly slower
- useful for testing different processor architectures

For example, an x86_64 system can emulate an ARM system using QEMU without requiring ARM hardware.

---

> **Common Mistake**
>
> Do not confuse **QEMU** with **KVM**.
>
> QEMU creates and manages the virtual machine.
>
> KVM accelerates CPU execution using hardware-assisted virtualization.

---

### Verification Commands

Check whether QEMU is installed:

```bash
qemu-system-x86_64 --version
```

List available QEMU binaries:

```bash
ls /usr/bin/qemu-*
```

Show running QEMU virtual machines:

```bash
ps -ef | grep qemu
```

---

> **Exam Tip**
>
> If an exam question asks which component creates the virtual machine process or provides virtual hardware devices, the correct answer is **QEMU**.
>
> If it asks which component provides hardware-assisted CPU virtualization, the answer is **KVM**.

---

## Review

After reading this section you should be able to answer the following questions:

- [ ] What does QEMU stand for?
- [ ] What are the primary responsibilities of QEMU?
- [ ] How do QEMU and KVM work together?
- [ ] What is the difference between QEMU with and without KVM?
- [ ] Which component creates the virtual machine process?

## libvirt

**libvirt** is an open-source virtualization management framework.

Unlike KVM or QEMU, libvirt does not provide virtualization itself.

Instead, it offers a common management interface for different virtualization technologies.

Applications can use libvirt to create, configure, start, stop and monitor virtual machines without interacting directly with the underlying hypervisor.

---

### Responsibilities

libvirt provides a standardized management layer.

Typical responsibilities include:

- creating virtual machines
- starting and stopping virtual machines
- configuring virtual hardware
- managing virtual networks
- managing virtual storage pools
- taking snapshots
- monitoring virtual machine status

libvirt communicates with the virtualization backend and exposes a consistent API to management applications.

---

### Relationship to KVM and QEMU

libvirt sits above the virtualization layer.

It communicates with QEMU, which in turn uses KVM for hardware-assisted virtualization.

```text
Management Applications
(virt-manager, virsh, OpenStack, etc.)
                │
             libvirt
                │
              QEMU
                │
         Linux Kernel + KVM
                │
            CPU Hardware
```

This layered architecture allows management applications to work without needing to know implementation details of the underlying virtualization technology.

---

### libvirtd

The central component of libvirt is the **libvirtd** daemon.

It listens for requests from management applications and performs the requested operations using the appropriate virtualization backend.

Examples include:

- starting virtual machines
- stopping virtual machines
- creating virtual networks
- managing storage pools

Modern Linux distributions may split libvirt functionality into several specialized daemons, but the LPIC-3 objectives primarily refer to the traditional **libvirtd** service.

---

### Common Tools

Several tools use libvirt.

| Tool | Purpose |
|------|---------|
| `virsh` | Command-line management tool |
| `virt-manager` | Graphical virtual machine manager |
| `virt-install` | Create new virtual machines |
| `virt-viewer` | Connect to a virtual machine console |

All of these communicate with libvirt instead of interacting directly with QEMU or KVM.

---

> **Common Mistake**
>
> libvirt is **not** a hypervisor.
>
> It is a management framework that communicates with virtualization backends such as QEMU/KVM.

---

### Verification Commands

Check whether libvirt is installed:

```bash
virsh --version
```

Show the status of the libvirt service:

```bash
systemctl status libvirtd
```

List all virtual machines:

```bash
virsh list --all
```

Display virtualization information:

```bash
virsh version
```

---

> **Exam Tip**
>
> If an exam question asks which component provides a common management API for virtualization, the correct answer is **libvirt**.
>
> If it asks which component executes virtual machines, the answer is **QEMU**.

---

## Review

After reading this section you should be able to answer the following questions:

- [ ] What is libvirt?
- [ ] Which responsibilities does libvirt have?
- [ ] How does libvirt interact with QEMU and KVM?
- [ ] Which tools commonly use libvirt?
- [ ] Why is libvirt not a hypervisor?

## Proxmox VE

**Proxmox Virtual Environment (Proxmox VE)** is an open-source virtualization management platform.

Unlike KVM, QEMU or libvirt, Proxmox VE is not responsible for virtualization itself.

Instead, it combines multiple virtualization technologies and provides a unified platform for deploying, managing and monitoring virtual machines and containers.

---

### Responsibilities

Typical responsibilities include:

- managing virtual machines
- managing Linux containers (LXC)
- providing a web-based management interface
- cluster management
- live migration
- storage management
- backup and restore
- high availability (HA)

Proxmox VE integrates several open-source components into a single management platform.

---

### Relationship to KVM, QEMU and libvirt

The Linux virtualization stack can be viewed as multiple layers.

```text
+--------------------------------------+
|          Proxmox VE                  |
|   (Management Platform)              |
+--------------------------------------+
|     QEMU / LXC Management            |
+--------------------------------------+
|      Linux Kernel + KVM              |
+--------------------------------------+
|            Hardware                  |
+--------------------------------------+
```

For virtual machines, Proxmox VE uses:

- **KVM** for hardware-assisted virtualization
- **QEMU** for the virtual machine process and virtual hardware

For operating-system-level virtualization, Proxmox VE uses:

- **LXC (Linux Containers)**

This allows a single platform to manage both virtual machines and Linux containers.

---

### Typical Components

A standard Proxmox VE installation includes components such as:

- KVM
- QEMU
- LXC
- Web GUI
- REST API
- Cluster services
- Backup services

The administrator interacts with Proxmox VE rather than managing KVM or QEMU directly.

---

> **Common Mistake**
>
> Proxmox VE is **not** a hypervisor.
>
> It is a management platform built on top of virtualization technologies such as KVM and QEMU.

---

### Verification Commands

Display the Proxmox VE version:

```bash
pveversion
```

Display detailed package versions:

```bash
pveversion -v
```

List virtual machines:

```bash
qm list
```

List Linux containers:

```bash
pct list
```

---

> **Exam Tip**
>
> If an exam question asks which component provides a complete virtualization management platform with clustering, backups and a web interface, the correct answer is **Proxmox VE**.
>
> KVM and QEMU provide virtualization; Proxmox VE provides management.

---

## Review

After reading this section you should be able to answer the following questions:

- [ ] What is Proxmox VE?
- [ ] Which virtualization technologies does Proxmox VE use?
- [ ] Why is Proxmox VE not a hypervisor?
- [ ] Which commands list virtual machines and containers?
- [ ] Which features does Proxmox VE provide beyond virtualization?

---

# Virtualization Techniques

## Full Virtualization

*TODO*

## Paravirtualization

*TODO*

## Hardware-assisted Virtualization

*TODO*

---

# Discovery Lab

*TODO*

---

# Knowledge Check

*TODO*

---

# Summary

*TODO*

---

# Review Checklist

- [ ] Official objective covered
- [ ] All required terminology explained
- [ ] KVM/QEMU/libvirt relationship understood
- [ ] Hypervisor types understood
- [ ] Discovery Lab completed
- [ ] Knowledge Check passed
- [ ] Virtual machine concepts understood
